# PostgreSQL database connection properties

spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=1

spring.datasource.driver-class-name=org.postgresql.Driver

# Specify the schema to use
spring.jpa.properties.hibernate.default_schema=xrs

# JPA and Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


##
### Set the root logger level to INFO for overall application logging
#logging.level.root=INFO
#
## Set the logging level to DEBUG for the Spring Web package, which includes request/response handling
#logging.level.org.springframework.web=DEBUG
#
#
#logging.console.enabled=true
#logging.file.enabled=true
#
## Enable Spring Boot's default exception handling, which includes detailed error information
#server.error.include-exception=true
#server.error.include-stacktrace=ALWAYS
